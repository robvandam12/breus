
import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export const ContratistaManager = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Gestión de Contratistas</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-gray-600">Módulo de gestión de contratistas en desarrollo.</p>
      </CardContent>
    </Card>
  );
};
