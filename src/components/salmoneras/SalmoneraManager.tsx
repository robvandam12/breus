
import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export const SalmoneraManager = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Gestión de Salmoneras</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-gray-600">Módulo de gestión de salmoneras en desarrollo.</p>
      </CardContent>
    </Card>
  );
};
